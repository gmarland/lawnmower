export interface VRModalConfig {
    baseImagePath: string;

    id: string;

    width: number;
    
    height: number;

    offset: number;

    padding: number;

    borderRadius: number;

    backgroundColor: string;

    borderColor: string;

    borderWidth: number;
    
    closeButtonWidth: number;
}